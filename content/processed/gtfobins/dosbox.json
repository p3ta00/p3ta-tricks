{"title": "dosbox", "source": "gtfobins", "source_label": "GTFOBins", "path": "dosbox", "html": "<h1>dosbox</h1>\n<h2>File Read</h2>\n<p>The file content will be displayed in the DOSBox graphical window.</p>\n<pre><code class=\"language-bash\">dosbox -c &#x27;mount c /&#x27; -c &#x27;type c:\\path\\to\\input&#x27;</code></pre>\n<p>The file is copied to a readable location.</p>\n<pre><code class=\"language-bash\">dosbox -c &#x27;mount c /&#x27; -c &#x27;copy c:\\path\\to\\input c:\\path\\to\\output&#x27; -c exit\ncat /path/to/OUTPUT</code></pre>\n<h2>File Write</h2>\n<p>Note that `echo` terminates the string with a DOS-style line terminator (`\\r\\n`), if that&#x27;s a problem and your scenario allows it, you can create the file outside `dosbox`, then use `copy` to do the actual write.</p>\n<pre><code class=\"language-bash\">dosbox -c &#x27;mount c /&#x27; -c &quot;echo DATA &gt;c:\\path\\to\\output&quot; -c exit</code></pre>", "excerpt": "dosbox File Read The file content will be displayed in the DOSBox graphical window. dosbox -c &#x27;mount c /&#x27; -c &#x27;type c:\\path\\to\\input&#x27; The file is copied to a readable location. dosbox -c &#x27;mount c /&#x27; -c &#x27;copy…", "tags": ["func:file-read", "func:file-write", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}