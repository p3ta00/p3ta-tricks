{"title": "time", "source": "gtfobins", "source_label": "GTFOBins", "path": "time", "html": "<h1>time</h1>\n<h2>Shell</h2>\n<p>Note that the shell might have its own builtin `time` implementation, which may behave differently than the binary, which is often located at `/usr/bin/time`.</p>\n<pre><code class=\"language-bash\">time /bin/sh</code></pre>\n<p><strong>SUID override:</strong></p>\n<pre><code class=\"language-bash\">time /bin/sh -p</code></pre>", "excerpt": "time Shell Note that the shell might have its own builtin `time` implementation, which may behave differently than the binary, which is often located at `/usr/bin/time`. time /bin/sh SUID override: time /bin/sh -p", "tags": ["func:shell", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}