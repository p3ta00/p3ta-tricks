{"title": "troff", "source": "gtfobins", "source_label": "GTFOBins", "path": "troff", "html": "<h1>troff</h1>\n<h2>File Read</h2>\n<p>The file is typeset but text is still readable in the output, alternatively the output can be read with `man -l`.</p>\n<pre><code class=\"language-bash\">troff /path/to/input-file</code></pre>", "excerpt": "troff File Read The file is typeset but text is still readable in the output, alternatively the output can be read with `man -l`. troff /path/to/input-file", "tags": ["func:file-read", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}