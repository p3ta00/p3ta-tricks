{"title": "Diskshadow.exe", "source": "lolbas", "source_label": "LOLBAS", "path": "diskshadow", "html": "<h1>Diskshadow.exe</h1>\n<p>Diskshadow.exe is a tool that exposes the functionality offered by the volume shadow copy Service (VSS).</p>\n<h2>Dump</h2>\n<p>Use diskshadow to exfiltrate data from VSS such as NTDS.dit</p>\n<pre><code class=\"language-powershell\">diskshadow.exe /s {PATH:.txt}</code></pre>\n<p>Execute commands using diskshadow.exe from a prepared diskshadow script. &mdash; <strong>MITRE:</strong> T1003.003 &mdash; <strong>Privileges:</strong> User</p>\n<h2>Execute</h2>\n<p>Use diskshadow to bypass defensive counter measures</p>\n<pre><code class=\"language-powershell\">diskshadow&gt; exec {PATH:.exe}</code></pre>\n<p>Execute commands using diskshadow.exe to spawn child process &mdash; <strong>MITRE:</strong> T1202 &mdash; <strong>Privileges:</strong> User</p>", "excerpt": "Diskshadow.exe Diskshadow.exe is a tool that exposes the functionality offered by the volume shadow copy Service (VSS). Dump Use diskshadow to exfiltrate data from VSS such as NTDS.dit diskshadow.exe /s {PATH:.txt} Execute commands using…", "tags": ["Dump", "Execute"]}