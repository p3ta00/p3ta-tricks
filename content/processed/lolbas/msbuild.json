{"title": "Msbuild.exe", "source": "lolbas", "source_label": "LOLBAS", "path": "msbuild", "html": "<h1>Msbuild.exe</h1>\n<p>Used to compile and execute code</p>\n<h2>AWL Bypass</h2>\n<p>Compile and run code</p>\n<pre><code class=\"language-powershell\">msbuild.exe {PATH:.xml}</code></pre>\n<p>Build and execute a C# project stored in the target XML file. &mdash; <strong>MITRE:</strong> T1127.001 &mdash; <strong>Privileges:</strong> User</p>\n<h2>Execute</h2>\n<p>Compile and run code</p>\n<pre><code class=\"language-powershell\">msbuild.exe {PATH:.csproj}</code></pre>\n<p>Build and execute a C# project stored in the target csproj file. &mdash; <strong>MITRE:</strong> T1127.001 &mdash; <strong>Privileges:</strong> User</p>\n<p>Execute DLL</p>\n<pre><code class=\"language-powershell\">msbuild.exe /logger:TargetLogger,{PATH_ABSOLUTE:.dll};MyParameters,Foo</code></pre>\n<p>Executes generated Logger DLL file with TargetLogger export. &mdash; <strong>MITRE:</strong> T1127.001 &mdash; <strong>Privileges:</strong> User</p>\n<p>Execute project file that contains XslTransformation tag parameters</p>\n<pre><code class=\"language-powershell\">msbuild.exe {PATH:.proj}</code></pre>\n<p>Execute JScript/VBScript code through XML/XSL Transformation. Requires Visual Studio MSBuild v14.0+. &mdash; <strong>MITRE:</strong> T1127.001 &mdash; <strong>Privileges:</strong> User</p>\n<p>Bypass command-line based detections</p>\n<pre><code class=\"language-powershell\">msbuild.exe @{PATH:.rsp}</code></pre>\n<p>By putting any valid msbuild.exe command-line options in an RSP file and calling it as above will interpret the options as if they were passed on the command line. &mdash; <strong>MITRE:</strong> T1036 &mdash; <strong>Privileges:</strong> User</p>", "excerpt": "Msbuild.exe Used to compile and execute code AWL Bypass Compile and run code msbuild.exe {PATH:.xml} Build and execute a C# project stored in the target XML file. &mdash; MITRE: T1127.001 &mdash; Privileges: User Execute Compile and run code…", "tags": ["AWL Bypass", "Execute"]}