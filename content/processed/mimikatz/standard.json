{"title": "standard (main module)", "source": "mimikatz", "source_label": "Mimikatz", "path": "standard", "html": "<h1 id=\"standard-main-module\">standard (main module)</h1>\n<p>Core mimikatz utility commands. These don't require a module prefix — <code>exit</code> works the same as <code>standard::exit</code>.</p>\n<hr>\n<h2 id=\"exit\">exit</h2>\n<p>Close mimikatz, running cleanup routines.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # exit\nBye!\n</code></pre></div>\n<hr>\n<h2 id=\"cls\">cls</h2>\n<p>Clear the console screen.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # cls\n</code></pre></div>\n<p>Note: doesn't work with remote execution tools like psexec or Meterpreter.</p>\n<hr>\n<h2 id=\"version\">version</h2>\n<p>Display mimikatz and Windows version information.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # version\n</code></pre></div>\n<hr>\n<h2 id=\"cd-change-directory\">cd — Change Directory</h2>\n<p>Change or display current working directory.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # cd [&lt;directory&gt;]\n</code></pre></div>\n<h3 id=\"examples\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code># Display current directory\nmimikatz # cd\n\n# Change directory\nmimikatz # cd C:\\temp\n</code></pre></div>\n<hr>\n<h2 id=\"log-log-output-to-file\">log — Log Output to File</h2>\n<p>Log all mimikatz output to a file.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # log [&lt;filename&gt;] [/stop]\n</code></pre></div>\n<h3 id=\"options\">Options</h3>\n<ul>\n<li><code>&lt;filename&gt;</code> — output file (default: <code>mimikatz.log</code>)</li>\n<li><code>/stop</code> — stop logging</li>\n</ul>\n<h3 id=\"examples_1\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code># Start logging to default file\nmimikatz # log\n\n# Log to specific file\nmimikatz # log C:\\temp\\output.log\n\n# Stop logging\nmimikatz # log /stop\n</code></pre></div>\n<hr>\n<h2 id=\"base64-base64-output-mode\">base64 — Base64 Output Mode</h2>\n<p>Toggle base64 encoding for file output (useful for in-memory operation — avoids writing files to disk).</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # base64 [/output:true|false]\n</code></pre></div>\n<h3 id=\"examples_2\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code># Enable base64 output\nmimikatz # base64\n\n# Combined with ticket export\nmimikatz # base64 /output:true\nmimikatz # kerberos::list /export\n</code></pre></div>\n<hr>\n<h2 id=\"sleep-sleep\">sleep — Sleep</h2>\n<p>Pause execution for a specified duration.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # sleep [&lt;milliseconds&gt;]\n</code></pre></div>\n<p>Default: 1000ms</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # sleep 4200\nSleep : 4200 ms... End !\n</code></pre></div>\n<hr>\n<h2 id=\"answer\">answer</h2>\n<p>Returns \"42\" (Douglas Adams reference).</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # answer\n42.\n</code></pre></div>\n<hr>\n<h2 id=\"coffee\">coffee</h2>\n<p>Displays ASCII art of a coffee cup.</p>\n<div class=\"highlight\"><pre><span></span><code>mimikatz # coffee\n</code></pre></div>\n<hr>\n<h2 id=\"notes\">Notes</h2>\n<ul>\n<li><code>standard::base64 /output:true</code> combined with <code>kerberos::list /export</code> or <code>sekurlsa::tickets /export</code> outputs tickets as base64 blobs instead of writing .kirbi files — useful for in-memory-only operations</li>\n<li>Combine <code>log</code> with attack sequences to capture all output for reporting</li>\n</ul>", "excerpt": "standard (main module) Core mimikatz utility commands. These don't require a module prefix — exit works the same as standard::exit . exit Close mimikatz, running cleanup routines. mimikatz # exit Bye! cls Clear the console screen. mimikatz # cls…", "tags": []}