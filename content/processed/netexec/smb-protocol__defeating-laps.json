{"title": "Defeating LAPS", "source": "netexec", "source_label": "NetExec Wiki", "path": "smb-protocol/defeating-laps", "html": "<h1 id=\"defeating-laps\">Defeating LAPS</h1>\n<h3 id=\"using-netexec-when-laps-installed-on-the-domain\">Using NetExec When LAPS Installed on the Domain</h3>\n<p>If LAPS is used inside the domain, it can be hard to use NetExec to execute a command on every computer on the domain.</p>\n<p>Therefore, a new core option has been added <code>--laps</code>! If you have compromised an account that can read LAPS password, you can use NetExec like this</p>\n<div class=\"highlight\"><pre><span></span><code>nxc<span class=\"w\"> </span>smb<span class=\"w\"> </span>&lt;ip&gt;<span class=\"w\"> </span>-u<span class=\"w\"> </span>&lt;username&gt;-can-read-laps<span class=\"w\"> </span>-p<span class=\"w\"> </span>&lt;password&gt;<span class=\"w\"> </span>--laps\n</code></pre></div>\n<p>{% hint style=\"info\" %}<br>\nIf the default administrator name is not <code>administrator</code> add the user after the option</p>\n<p><code>--laps name</code><br>\n{% endhint %}</p>", "excerpt": "Defeating LAPS Using NetExec When LAPS Installed on the Domain If LAPS is used inside the domain, it can be hard to use NetExec to execute a command on every computer on the domain. Therefore, a new core option has been added --laps ! If you have…", "tags": []}